<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="Clever_Vpn.Pages.HomePage.components.TrafficCard"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Clever_Vpn.Pages.HomePage.components"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Loaded="UserControl_Loaded"
    Unloaded="UserControl_Unloaded"
    mc:Ignorable="d">

    <UserControl.Resources>
        <Style x:Key="TrafficPanelStyle" TargetType="StackPanel">
            <Setter  Property="HorizontalAlignment" Value="Center" />
            <Setter  Property="VerticalAlignment" Value="Center" />
        </Style>
        <Style x:Key="TrafficTextBlockStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="16" />
            <Setter Property="FontWeight" Value="Bold" />

        </Style>
        <Style x:Key="TrafficIconStyle" TargetType="FontIcon">
            <Setter Property="Foreground" Value="LightGray" />
            <Setter Property="FontSize" Value="24" />
            <Setter Property="FontWeight" Value="ExtraBold" />
        </Style>
    </UserControl.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <StackPanel Style="{StaticResource TrafficPanelStyle}">
            <FontIcon Glyph="&#xE898;" Style="{StaticResource TrafficIconStyle}"/>
            <TextBlock x:Name="TxTextBlock" Style="{StaticResource TrafficTextBlockStyle}" />
        </StackPanel>
           
            <Rectangle Grid.Column="1" Width="2" VerticalAlignment="Stretch"  Fill="{ThemeResource ControlAltFillColorQuarternaryBrush}" Margin="0,10" />
        <StackPanel Grid.Column="2" Style="{StaticResource TrafficPanelStyle}" >
            <FontIcon Glyph="&#xE896;" Style="{StaticResource TrafficIconStyle}" />
            <TextBlock x:Name="RxTextBlock" Style="{StaticResource TrafficTextBlockStyle}" />
        </StackPanel>
    </Grid>
</UserControl>
