<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="Clever_Vpn.Pages.HomePage.components.HomeCard"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Clever_Vpn.Pages.HomePage.components"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:media="using:Microsoft.UI.Xaml.Media"
    mc:Ignorable="d">

    <Grid Background="{StaticResource LayerFillColorAltBrush}" BorderThickness="1" BorderBrush="{StaticResource ControlAltFillColorQuarternaryBrush}">

        <StackPanel Orientation="Vertical" HorizontalAlignment="Stretch" VerticalAlignment="Center">
            <local:HomeCardIcon HorizontalAlignment="Center"/>
            <local:HomeCardDivider  Margin="0,20,0,10" />
            <local:HomeCardLocationSelector />
            <ToggleSwitch 
                x:Name="OnOffSwitch"
                Toggled="OnToggled"
                IsOn="{x:Bind UpdateToggleState(Vm.VpnState), Mode=OneWay}" 
                IsEnabled="{x:Bind UpdateToggleEnable(Vm.VpnState), Mode=OneWay}" 
                OnContent="" OffContent="" HorizontalAlignment="Center" MinWidth="0" MinHeight="0" />
        </StackPanel>
    </Grid>
</UserControl>
