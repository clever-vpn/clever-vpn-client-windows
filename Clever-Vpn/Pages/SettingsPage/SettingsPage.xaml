<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="Clever_Vpn.Pages.SettingsPage.SettingsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Clever_Vpn.Pages.SettingsPage"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:c="using:Clever_Vpn.components"
    xmlns:c1="using:Clever_Vpn.Pages.SettingsPage.componets"
    xmlns:utils="using:Clever_Vpn.utils"
    xmlns:vpn="using:Clever_Vpn_Windows_Kit.Data"
    mc:Ignorable="d">

    <Grid     
        RequestedTheme="Light"
        >
        <c:Layout>
            <c:Layout.TopBar>
                <c:TopBar x:Name="TopBar" >
                    <c:TopBar.LeftContent>
                        <AppBarButton Icon="Back"  
                                      Width="48" Height="48"
                                      Click="OnBackClick" />
                    </c:TopBar.LeftContent>
                    <TextBlock x:Uid="SettingTitle"  Style="{StaticResource SubtitleTextBlockStyle}" />
                </c:TopBar>
            </c:Layout.TopBar>
            <ScrollViewer>
                <StackPanel >
                    <c1:SettingPanel x:Uid="Key">
                        <StackPanel>
                            <TextBlock Text="{x:Bind FormatKey(Vm.UserInfo.Key) }" />
                            <c:Divider/>
                            <Button Command="{x:Bind  Vm.DeActivateCommand}" Style="{ThemeResource AccentButtonStyle}" x:Uid="DeActivate" />
                        </StackPanel>
                    </c1:SettingPanel>
                    <c1:SettingPanel x:Uid="ProtocolType">
                        <ListView 
                            Loaded="OnProtocolSettingLoaded"
                            IsItemClickEnabled="True"
                            ItemClick="OnProtocolItemClick"
                            SelectionMode="None">
                            <ListView.ItemTemplate>
                                <DataTemplate x:DataType="vpn:ProtocolType">
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="Auto" />
                                            <ColumnDefinition Width="*" />
                                            <ColumnDefinition Width="Auto" />
                                        </Grid.ColumnDefinitions>
                                        <TextBlock Text="{x:Bind }" Style="{ThemeResource BodyStrongTextBlockStyle}" />
                                        <TextBlock Grid.Column="1" Margin="10,0,0,0" Style="{ThemeResource CaptionTextBlockStyle}"
                                                Text="{x:Bind local:SettingsPage.TipOfProtocolType((vpn:ProtocolType))}" />
                                        <!-- Check icon if selected -->
                                        <c1:SelectedIcon ProtocolType="{x:Bind}"  Grid.Column="2"/>
                                    </Grid>
                                </DataTemplate>
                            </ListView.ItemTemplate>
                        </ListView>
                    </c1:SettingPanel>

                    <c1:SettingPanel x:Uid="RunAtStartup">

                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto" />
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>
                            <TextBlock Grid.Column="0" VerticalAlignment="Center" FontSize="14" x:Uid="RunAtStartup1" />

                                <ToggleSwitch x:Name="StartupToggle"
                                    VerticalAlignment="Center"
                                    MinWidth="0" 
                                    Grid.Column="2"
                                    OffContent="Off"
                                    OnContent="On"
                                    Toggled="OnAutoStartupToggle"           
                                    />
                            </Grid>

                    </c1:SettingPanel>
                    <c1:SettingPanel x:Uid="Logs">
                        <Button Click="OnLogPage" x:Uid="LogsButton" />
                    </c1:SettingPanel>
                    <c1:SettingPanel x:Uid="AboutUs">
                        <RichTextBlock>
                            <Paragraph>
                                <Run x:Uid="Website" />
                                <Hyperlink  NavigateUri="{x:Bind Vm.UserInfo.Url}">
                                    <Run Text="{x:Bind Vm.UserInfo.Url}" />
                                </Hyperlink>
                            </Paragraph>
                        </RichTextBlock>
                    </c1:SettingPanel>
                    <c1:SettingPanel x:Uid="Version" >
                        <TextBlock Text="{x:Bind GetVersion}" />
                    </c1:SettingPanel>
                </StackPanel>
            </ScrollViewer>
        </c:Layout>


    </Grid>

</Page>
