<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="Clever_Vpn.components.TopBar"
    x:Name="Root"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Clever_Vpn.components"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">


    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Left section -->
        <StackPanel Orientation="Horizontal"
                VerticalAlignment="Center"
                Spacing="8"
                Grid.Column="0">
            <ContentPresenter Content="{x:Bind LeftContent}" />
        </StackPanel>

        <!-- Center section -->
        <StackPanel Orientation="Horizontal"
                HorizontalAlignment="Center"
                VerticalAlignment="Center"
                Spacing="8"
                Grid.Column="1">
            <ContentPresenter Content="{x:Bind CenterContent}"  />
        </StackPanel>

        <!-- Right section -->
        <StackPanel Orientation="Horizontal"
                HorizontalAlignment="Right"
                VerticalAlignment="Center"
                Spacing="8"
                Grid.Column="2">
            <ContentPresenter Content="{x:Bind RightContent}" />
        </StackPanel>


    </Grid>

</UserControl>
