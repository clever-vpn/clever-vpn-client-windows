<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="Clever_Vpn.components.Layout"
    x:Name="Root"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Clever_Vpn.components"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid>
        <!-- 定义三部分，TopBar Content Footer -->
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- TopBar -->
        <Grid  Grid.Row="0">
            <ContentPresenter Content="{x:Bind TopBar}" />
        </Grid>

        <!-- Content -->
        <Grid Grid.Row="1">
            <ContentPresenter Content="{x:Bind Body }" />
        </Grid>

        <!-- Footer -->
        <Grid Grid.Row="2">
            <ContentPresenter Content="{x:Bind Footer}" />
        </Grid>

    </Grid>
</UserControl>
