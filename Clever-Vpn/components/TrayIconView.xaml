<UserControl
    x:Class="Clever_Vpn.components.TrayIconView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:tb="using:H.NotifyIcon"
    mc:Ignorable="d"
    >

    <tb:TaskbarIcon
        x:Name="TrayIcon"
        x:Uid="TrayIcon"
        x:FieldModifier="public"
        IconSource="{x:Bind GetIconUrl(vm.VpnState), Mode=OneWay}"
        ContextMenuMode="PopupMenu"
        LeftClickCommand="{x:Bind ShowHideWindowCommand}"
        NoLeftClickDelay="True"
        ToolTipText="Wellcome to Clever Vpn"
        >
        <tb:TaskbarIcon.ContextFlyout>
            <MenuFlyout AreOpenCloseAnimationsEnabled="False">
                <MenuFlyoutItem
                    x:Uid="TrayMenuShowHide"
                    Command="{x:Bind ShowHideWindowCommand}"
                    Text="Show/Hide Window"
                    />
                <MenuFlyoutSeparator />
                <MenuFlyoutItem
                    x:Uid="TrayMenuExit"
                    Command="{x:Bind ExitApplicationCommand}"
                    Text="Exit"
                    />
            </MenuFlyout>
        </tb:TaskbarIcon.ContextFlyout>
    </tb:TaskbarIcon>

</UserControl>
