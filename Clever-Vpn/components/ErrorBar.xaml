<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="Clever_Vpn.components.ErrorBar"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Clever_Vpn.components"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:triggers="using:CommunityToolkit.WinUI"
    mc:Ignorable="d">

    <Grid>
        <VisualStateManager.VisualStateGroups>
            <VisualStateGroup>
                <VisualState x:Name="HasError">
                    <VisualState.StateTriggers>
                        <triggers:IsNullOrEmptyStateTrigger Value="{x:Bind vm.LastError, Mode=OneWay}" />
                    </VisualState.StateTriggers>
                    <VisualState.Setters>
                        <Setter Target="ErrorBar1.IsOpen" Value="False" />
                    </VisualState.Setters>
                </VisualState>
                <VisualState x:Name="NoError">
                    <VisualState.StateTriggers>
                        <triggers:IsNotEqualStateTrigger
                            Value="{x:Bind  vm.LastError, Mode=OneWay}"
                            To="{x:Null}" />
                    </VisualState.StateTriggers>
                    <VisualState.Setters>
                        <Setter Target="ErrorBar1.IsOpen" Value="True" />
                    </VisualState.Setters>
                </VisualState>
            </VisualStateGroup>
        </VisualStateManager.VisualStateGroups>
        <InfoBar
            x:Name="ErrorBar1"
            CloseButtonCommand="{x:Bind vm.ClearLastErrorCommand}"
            Severity="Error"
            Title="Title"
            Message="Essential app message for your users to be informed of, acknowledge, or take action on." />
    </Grid>
</UserControl>
